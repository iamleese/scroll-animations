{
	"name": "scroll-animations",
	"version": "0.1.0",
	"description": "Uses Locomotive Scroll attributes to blocks to create smooth scrolling and parallax effects.",
	"author": "Melissa Hiatt",
	"license": "GPL-2.0-or-later",
	"main": "build/index.js",
	"scripts": {
		"build": "concurrently \"webpack --config webpack.config.js --mode=production\" \"wp-scripts build\" ",
		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"packages-update": "wp-scripts packages-update",
		"plugin-zip": "wp-scripts plugin-zip",
		"start": "concurrently \"webpack --config webpack.config.js --mode=development\" \"wp-scripts start\" ",
		"test": "jest"
	},
	"devDependencies": {
		"@wordpress/scripts": "^25.4.0"
	},
	"dependencies": {
		"concurrently": "^7.6.0",
		"locomotive-scroll": "^4.1.4"
	}
}
